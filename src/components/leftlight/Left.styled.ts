import styled from "styled-components";

export const Lightstyled = styled.div`
.postLight {
      position: fixed;
      top: 0px;
      left: 0;
      /* border: 2px red solid; */
      z-index: -5;
    }
`